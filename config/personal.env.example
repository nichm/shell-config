# =============================================================================
# 📝 PERSONAL CONFIGURATION - Fill out and save as config/personal.env
# =============================================================================
# This is the ONLY file you need to edit to personalize shell-config.
# Copy this file:  cp config/personal.env.example config/personal.env
# Then fill in your values below.
#
# This file is loaded by lib/core/personal.sh at shell startup.
# config/personal.env is gitignored — your data stays local.
# =============================================================================

# =============================================================================
# 👤 IDENTITY (used by gitconfig, CODEOWNERS, docs)
# =============================================================================

# Your full name (used in git commits)
GIT_USER_NAME="Your Name"

# Your email (used in git commits)
GIT_USER_EMAIL="your-email@example.com"

# Your GitHub username (used for CODEOWNERS, PR lookups, repo URLs)
GITHUB_USERNAME="your-github-username"

# Your GitHub org or personal account for repos (used in clone URLs, PR lookups)
# If you use a personal account, set this to your username
GITHUB_ORG="your-github-org"

# =============================================================================
# 🖥️ SERVERS (optional — add your SSH server aliases)
# =============================================================================
# Format: ALIAS=USER@HOST
# These are created as shell aliases: alias ALIAS='ssh USER@HOST'

# SERVER_1_ALIAS="my-vps"
# SERVER_1_TARGET="root@203.0.113.1"

# SERVER_2_ALIAS="staging"
# SERVER_2_TARGET="deploy@198.51.100.5"

# SERVER_3_ALIAS="production"
# SERVER_3_TARGET="ubuntu@192.0.2.10"

# =============================================================================
# 📊 GHLS PROJECT COLORS (optional — customize folder display)
# =============================================================================
# Format: PROJECT_NAME|EMOJI|BG_COLOR_256|FG_COLOR_256
# These add custom emoji and colors to the ghls (GitHub ls) display.
# If not set, projects auto-detect based on package.json, Cargo.toml, etc.
#
# Color reference: https://en.wikipedia.org/wiki/ANSI_escape_code#8-bit
#
# Example entries:
# GHLS_PROJECT_1="my-api|📊|25|255"
# GHLS_PROJECT_2="my-webapp|🚀|202|255"
# GHLS_PROJECT_3="my-cli|🔧|94|255"

# =============================================================================
# 🏷️ GITHUB REPOS (optional — for apply-master-labels.sh)
# =============================================================================
# Space-separated list of your GitHub repository names
# Used by .github/setup/apply-master-labels.sh to sync labels

# GITHUB_REPOS="repo-1 repo-2 repo-3"
